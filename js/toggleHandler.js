const toggleHandler = (id) => {
  const newTodos = todos.map((todo) => {
    if (todo.id === id) {
      id: todo.id, task;
      todo.task, date;
      todo.date, completed;
      !todo.completed;
    }
  });
};
